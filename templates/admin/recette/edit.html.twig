{% extends 'admin/admin.html.twig' %}

{% block title %}Modification | {{recette.title}}{% endblock %}

{% block body %}
    <h1>Modifier la recette : {{recette.title}}</h1>

        <div class="mb-2">
            {{ include('recette/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
        
        <div class="d-flex gap-2">
    <a href="{{ path('recette.index') }}" class="btn btn-warning">Retourner aux recettes</a>
    
    {{ include('recette/_delete_form.html.twig') }}
</div>
{% endblock %}
