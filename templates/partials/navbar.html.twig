<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container">
		<a class="navbar-brand" href="{{path("home")}}">Cookify</a>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">

			<ul class="navbar-nav">
				<li class="nav-item">
					<a href="{{path("home")}}" class="nav-link {{app.current_route == "home" ? " active" : " "}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a href="{{path('contact')}}" class="nav-link {{app.current_route == "contact" ? " active" : " "}}">Contact</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
