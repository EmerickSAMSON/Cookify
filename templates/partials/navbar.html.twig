<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Cookify</a>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">

			<ul class="navbar-nav">
				<li class="nav-item">
					<a href="{{path("home")}}" class="nav-link {{app.current_route == "home" ? " active" : " "}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a href="{{path('recette.index')}}" class="nav-link {{app.current_route starts with "recette." ? " active" : " "}}">Recettes</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
